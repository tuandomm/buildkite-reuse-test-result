#!/bin/bash

set -ueo pipefail

n=${BUILDKITE_BUILD_NUMBER}

checksum=`cat /tmp/checksumid${n}.txt`

env > /tmp/test-result${n}
# todo: S3 bucket, repository name as plugin parameter
aws s3 cp /tmp/test-result${n} s3://mm-dev-syd-buildkite-artifacts/mmgp-frontend/test-results/$checksum
